#include "main.h"

/**
 * _strstr - finds the first occurrence of a substring.
 * @haystack: entire string.
 * @needle: substring.
 * Return: pointer to the beginning of located substring and NULL otherwise.
*/
char *_strstr(char *haystack, char *needle)
{
	return (0);
}

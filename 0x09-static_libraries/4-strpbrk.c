#include "main.h"

/**
 * _strpbrk - search inside any given string for any of pair of bytes.
 * @s: initial string.
 * @accept: second string.
 * Return: pointer to the byte in string that matches one of the
 * bytes and NULL otherwise
 */
char *_strpbrk(char *s, char *accept)
{
	return ('\0');
}
